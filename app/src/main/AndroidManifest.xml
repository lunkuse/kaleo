<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/checklogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/checklogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kaleo"
        tools:targetApi="31">
        <!--splash screen activity-->
        <activity
            android:name=".ui.splash.SplashScreenActivity"
            android:theme="@style/SplashTheme"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--main screen activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Kaleo.NoActionBar">

        </activity>
        <activity android:name=".ui.login.SignInActivity" />
        <activity android:name=".ui.signup.SignUpActivity" />
        <activity android:name=".ui.password.ForgottenActivity" />
    </application>

</manifest>